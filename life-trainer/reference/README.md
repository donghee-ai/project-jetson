# REFERENCE — 참고 자료 (git 제외)

같은 문제를 다르게 푼 외부 자료를 두는 곳. **내용물은 커밋하지 않는다** (이 파일만 추적).
남의 저장소 코드·명세를 우리 git 히스토리에 섞지 않기 위해서다.
저장소 루트의 `refs/reference/` 가 타 프로젝트 클론을 제외하는 것과 같은 원칙.

## 지금 들어 있는 것 (2026-08-17)

| 경로 | 정체 | 우리가 흡수한 것 |
|---|---|---|
| `harugyeol/` | Next.js 프론트 데모 (`DEMO MODE`, 스키마 비어 있음) | UI 언어 — 종이 질감 토큰, NOW 라인, 완료율 링, 주간 뷰, 바텀시트 |
| `slackbot2/` | PDA-Jetson 명세 (SPEC 1,284줄 + schema 547줄). **구현 없음** | 06:00 하루 경계, 계획 6단계 상태 + 이월 체인, 슬래시 명령 세트, `device` 차원 |
| `slackbot1/` | StudyLog — 동작하는 FastAPI 구현 | HMAC 원타임 링크 + 세션 (폰에서 Tailscale 없이) |

원본 zip 3개도 같은 디렉터리에 있다.

## 흡수 판단의 근거

셋 다 사람이 직접 입력하는 플래너다. **자동 계측이 실제로 도는 것은 우리뿐**이라
(`slackbot2` 는 설계만, 나머지 둘은 수동 전용) 우리를 뼈대로 두고 개념만 가져왔다.

무엇을 왜 가져왔고 무엇을 왜 버렸는지는
[../docs/implementation-plan.md](../docs/implementation-plan.md) 에 기록한다.

## 디자인 레퍼런스

`harugyeol/docs/references/ref1~3.png` 는 종이 스터디 플래너(motemote) 실물과 사용례다.
`../docs/refs/reference/planner_example.png` 와 같은 계열. 여기서 확인한 것:

- **TIMETABLE 은 24행 × 6칸 = 144칸, 06:00 시작** — 우리 10분 슬롯과 정확히 같다
- 체크 기호 3종: 완료 `v` · 진행 중 `/` · 내일로 연기 `>`
- 과제 목록과 타임테이블이 **과목 색으로 연결**된다
- 헤드라인 숫자는 커버리지가 아니라 **TOTAL TIME**
- 실사용(ref3)에서는 하위 항목 중첩(`└`)이 흔하고, 타임테이블은 한 색으로만 칠하는 경우가 많다

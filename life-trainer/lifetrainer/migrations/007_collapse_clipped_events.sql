-- 007: aw-server 가 질의 창에 맞춰 잘라 준 조각들을 한 행으로 합친다
--
-- ★ db.py 와 이 파일이 어긋나면 **db.py 가 맞다** (002 머리말과 같은 규칙).
--   실제 실행은 `_apply_007_collapse_clipped_events` 가 한다 — 겹침 판정이
--   SQL 한 줄로 안 떨어져서 파이썬으로 돈다.
--
-- 무엇을 합치나: 같은 `(bucket_id, event_id)` 이면서 **구간이 겹치거나 맞닿는** 행들.
--   겹침 조건이 안전장치다. `event_id` 는 재사용된다 —
--   안드로이드 버킷에서 id가 뒤로 점프하고, PC afk에서도 사라진 id가 다른 시간대에
--   다시 쓰이는 회귀가 있었다. 겹칠 때만 합쳐야 가짜 구간이 안 생긴다.
--
-- 파생 테이블을 **비우지 않는다.** 합치는 것은 원천(`aw_event`)이고 롤업은
-- 어차피 슬롯마다 잘라 흡수하고 있었다 — 화면 숫자는 안 바뀐다.

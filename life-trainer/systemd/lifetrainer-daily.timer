[Unit]
Description=Life Trainer — 매일 23:30 일일 리포트

[Timer]
# config/lifetrainer.toml 의 [report].daily_at 기본값(23:30)과 맞춰뒀다.
# 값을 바꿨다면 이 파일도 같이 고치고 `systemctl --user daemon-reload` 할 것.
OnCalendar=*-*-* 23:30:00
RandomizedDelaySec=5min
Persistent=true

[Install]
WantedBy=timers.target

# 이 저장소에서 일하는 규칙

Life Trainer 앱 안의 규칙은 [`Life_Trainer/CLAUDE.md`](Life_Trainer/CLAUDE.md) 에 있다.
여기는 **저장소 전체** — 측정 도구(`bench/`) · 런타임(`runtime/`) · CI · 문서 규칙이다.

---

## 1. 검사·경보를 만들 때 — **"언제 안 울리나"가 어려운 쪽이다**

**2026-08-28 하루에 이 부류를 다섯 번 밟았다.** 전부 "감시를 붙였다" 는 작업이었고,
전부 감시가 무력해지는 방향으로 틀렸다.

| | 무엇을 만들었나 | 어떻게 틀렸나 |
|---|---|---|
| 1 | `lt doctor` 큐 판정 | **누적** 실패율이라 원인을 고쳐도 2주간 안 꺼졌다. 그 사이 새 실패와 구분이 안 된다 ([HISTORY](Life_Trainer/HISTORY/2026-08-28-a-ratio-that-could-not-fall.md)) |
| 2 | `verify-boot.sh` journal 검사 | **옳지만 아직 증명 못 한 상태**(영속화 직후)를 실패로 셌다. 다음 재부팅까지 빨간불이 상수가 됐을 것 |
| 3 | `host-status.sh` OOM 집계 | 같은 사건을 **두 번 세어**(커널 줄 + 유저매니저 줄) 거짓 경보를 냈다 |
| 4 | 일일 점검 알림 | **이미 아는 WARN** 을 매일 다시 알릴 뻔했다. 일주일이면 사람이 채널을 끈다 |
| 5 | CI `shellcheck` job | **이 기기에서 돌릴 수 없게** 만들어, 추가 후 push 다섯 번이 빨간불인 걸 몰랐다 |

1~4 는 *"안 울려야 할 때 울린다"*, 5 는 *"울리는데 아무도 안 본다"* 다.
**방향은 반대인데 결과는 같다 — 신호가 죽는다.**

### 붙이기 전에 답할 것

- [ ] **이 신호는 언제 꺼지나?** 꺼지는 조건을 한 줄로 못 쓰면 만들지 않는다
- [ ] **원인을 고친 직후** 이 판정은 뭐라고 하나? *(→ 1번)*
- [ ] **옳지만 아직 증명 못 한 상태**를 뭐라고 하나? 실패와 미확인은 다르다 *(→ 2번)*
- [ ] 같은 사건을 **두 번 세지 않나?** 로그는 계층마다 같은 일을 적는다 *(→ 3번)*
- [ ] **이미 아는 문제**를 매번 다시 알리나? 알림은 **상태가 바뀔 때** 보낸다 *(→ 4번)*
- [ ] 이 검사를 **여기서 돌릴 수 있나?** 없으면 돌 수 있게 만든다 *(→ 5번)*
- [ ] 표본이 적을 때 비율을 과대 해석하지 않나? (3건 중 1건은 33% 가 아니다)

### 안 울려야 하는 예제를 같이 둔다

`bench/check-docs.sh` 는 **자기 시험지를 갖는다** — `bench/fixtures/check-docs/` 에
`must-fail.md`(반드시 잡아야 함) 와 `must-pass.md`(반드시 통과시켜야 함) 가 있고
`--self-test` 가 둘 다 돌린다.

**오탐이 나오면 그 예제를 `must-pass` 에 추가한다.** 헐거워지면 못 잡고,
빡빡해지면 사람이 꺼 버린다 — 둘 다 검사가 없는 것과 같다.

---

## 2. "만들었다" 와 "그게 실제로 불린다" 는 다르다

같은 날 이것도 세 번 나왔다:

- `make verify` 가 *"→ environment.md 갱신"* 이라 적고 **화면에만 찍었다.**
  그 사이 검증기 버그가 만든 ❌ 넷이 실측으로 실려 있었다
- OpenClaw 를 시스템 Node 로 설치했는데 **PATH 에서 nvm 이 앞이라** 여전히 옛 것이 불렸다.
  `use-system-node.sh` 는 그 PATH 로 `openclaw` 를 찾아 **방금 한 일을 되돌렸다**
- 게이트웨이가 시스템 Node 로 **실행**되는데 실행하는 **스크립트**는 nvm 안이었다.
  `lt doctor` 가 CLI 경로만 봐서 **절반만 옮겨진 상태**가 안 드러났다

이 저장소가 이미 아는 부류다 — *살아 있다 ≠ 서빙 가능하다*, *툴을 불렀다 ≠ 일을 했다*.

- [ ] 경로가 아니라 **실물**을 본다: `readlink -f /proc/<pid>/exe`
- [ ] PATH 에 기대지 말고 **절대경로로 고정**한다 (`openclaw_bin` 이 그래서 생겼다)
- [ ] 스크립트가 도구를 부를 때 **어느 것을 부르는지** 명시한다

### 프로세스를 잡을 때 (실제로 두 번 헤맸다)

```bash
pgrep -f openclaw          # ❌ **측정 스크립트 자신**을 잡는다 (README 의 pkill -f 함정)
pgrep -f "openclaw agent"  # ❌ 항상 떠 있는 게이트웨이를 잡는다
pgrep -P "$PARENT_PID"     # ✅ 부모의 자식만 본다
```

---

## 3. 숫자는 문서에 옮겨 적지 않는다

같은 값이 서로 다르게 적힌 사고를 **네 번** 겪었고, 세 번의 규칙 강화가 전부 실패했다.
네 번째에 명령으로 대체했고, **다섯 번째에 검사로 만들었다**:

```bash
make check-docs   # 문서가 운영 지표를 옮겨 적었는지. CI 와 pre-push 가 부른다
make status       # 앱 현황          make host-status  # 기기 현황
```

경위와 예외 규칙은 [`Life_Trainer/CLAUDE.md`](Life_Trainer/CLAUDE.md) §검증 에 있다.

---

## 4. 검증이 산출물을 더럽히면 안 된다

`make recovery-bundle` 이 DB 를 `quick_check` 하려고 열었더니 `-wal`·`-shm` 이 옆에
생겨 **그대로 묶였다.** 그건 *"동작 중인 WAL DB 를 복사한 것"* 과 구분이 안 되는 상태 —
`docs/runbook-backup-restore.md` 를 쓰게 만든 바로 그 결함이다.

- [ ] 검사 뒤 **부산물을 지운다**
- [ ] 검사는 가능하면 **사본에서** 한다

---

## 5. 되돌릴 수 없는 것은 순서를 지킨다

- **백업·복원이 갖춰지기 전에 BSP·커널을 건드리지 않는다.** 되돌아갈 길이
  재플래시 + 복원인데 그 재료가 같은 디스크에만 있으면 길이 아니다
- **옮길 때 버전을 고정한다.** 옮기는 김에 올리면 무엇이 원인인지 못 가른다
- **옛 설치본을 먼저 지우지 않는다.** 둘이 공존하는 동안에만 되돌릴 수 있다
- 지우기 전에 **안을 본다.** nvm Node 를 통째로 지울 뻔했는데 `@playwright` 가 거기 있었다

---

## 6. 새 스크립트를 어디 두나

**`bench/` 는 이름이 "측정" 인데 지금 27개 중 12개가 측정이 아니다.**
2026-08-28 하루에 운영·검사 9개가 들어갔다 — **막는 규칙이 없어서** 들어갔다.

- [ ] 이건 **재는 것인가, 돌리는 것인가?**
      · 재는 것 = 가끔 돌리고 결과가 `results/` 로 간다 → `bench/`
      · 돌리는 것 = 매일·매 push 돌고 결과가 화면·알림으로 간다 → **`bench/` 에 넣지 않는다**
- [ ] 돌리는 것이면 `Makefile` 진입점을 같이 만든다. 스크립트만 두면 아무도 안 부른다

목표 구조(`measure/` · `operate/` 분리)와 언제 옮길지는
[`folder-structure.md`](folder-structure.md) 에 있다. **옮기기 전까지는 이 질문으로 버틴다.**

---

## 7. 진입점

```bash
make check-fast   # 링크 + 문서 지표 + shellcheck (즉시. pre-push 훅이 부른다)
make check        # + 유닛 정적검사 + 테스트
make hooks        # pre-push 훅 설치 — git 이 훅을 안 따라가므로 기기마다 한 번
```

**로컬에서 못 도는 검사는 만들지 않는다** (§1-5). `shellcheck` 은 apt 판이 이 기기에
없어서 `shellcheck-py` 를 dev 의존성에 넣었다.

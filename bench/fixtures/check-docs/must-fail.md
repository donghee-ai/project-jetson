# 잡혀야 하는 것들

이 파일은 `check-docs.sh` 가 **반드시 실패시켜야** 하는 예제다.
하나라도 통과하면 검사기가 헐거워진 것이다.

- 이벤트 28,354 건이 쌓였다
- 문서 6,305건 (요약 1,657)
- 테스트 1,177개 통과
- GPU 잡 done 1,679 · failed 203
- 분류 규칙 47개
- make links 로 링크 435 개 검사
- 타이머 7개와 상시 서비스 3개가 돈다
- systemd 유닛 파일 17개
- 서비스 6개 active

<!-- 2026-08-31: `개` 없이 쓴 유닛 수. 이 형태를 놓쳐 architecture.md 가 옛 값을 들고 있었다 -->
systemd 디렉터리에는 타이머 7 + 서비스 10 이 있다.

<!-- 2026-08-31: 아래 다섯은 중복을 지우다 **낡은 채로** 발견됐는데 검사기가 전부 통과시켰다.
     `passed` 가 붙은 테스트 수만 보고, `항목`·괄호로 닫은 형태를 안 봤기 때문이다. -->
- `pytest tests/ -q` 로 1,136개
- ├── tests/          47 파일 · 15,863줄 · 1,136개
- HISTORY/ 에 28 문서
- 버그 21건을 색인에 남겼다
- `lt doctor` 17항목 점검
- 분류 규칙 47항목 · rules.yaml(규칙 47)
